// https://www.codewars.com/kata/55cbc3586671f6aa070000fb

// varian one
let checkForFactor = (base, factor) => base % factor === 0;

// variant two
let checkForFactor2 = (base, factor) => !(base % factor);

